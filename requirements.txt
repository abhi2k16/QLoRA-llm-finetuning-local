# ─────────────────────────────────────────────────────────────────────────────
#  requirements.txt — Local LLM Fine-Tuning
#  Target: Windows 11, Python 3.11 (win_llm conda env), CUDA 12.1
#  Stack:  HuggingFace transformers + PEFT + bitsandbytes (no Unsloth)
#
#  Install in this exact order:
#
#  Step 1 — Create and activate the conda environment:
#    conda create -n win_llm python=3.11 -y
#    conda activate win_llm
#
#  Step 2 — PyTorch with CUDA 12.1 (must come first):
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
#  Step 3 — bitsandbytes Windows community build:
#    pip install bitsandbytes --prefer-binary --extra-index-url https://jllllll.github.io/bitsandbytes-windows-webui
#
#  Step 4 — Remaining dependencies:
#    pip install -r requirements.txt
#
#  Step 5 — Fix SSL cert if HuggingFace downloads fail:
#    pip install certifi
#    python -c "import certifi; print(certifi.where())"
#    # Copy the printed path and run:
#    # $env:SSL_CERT_FILE = "<path printed above>"
# ─────────────────────────────────────────────────────────────────────────────

# HuggingFace ecosystem
transformers>=4.40.0
datasets>=2.18.0
accelerate>=0.28.0

# LoRA adapter support
peft>=0.10.0

# Config parsing
pyyaml>=6.0

# SSL fix for HuggingFace downloads on Windows
certifi

# Optional experiment tracking — uncomment to enable:
# wandb
# tensorboard

# ─── Do NOT add these here ───────────────────────────────────────────────────
# torch       — install with CUDA index URL (Step 2 above)
# bitsandbytes — install with Windows index URL (Step 3 above)
# unsloth     — segfaults on GTX 1050 (Pascal / compute 6.1); not used
# trl         — not needed; project uses a raw PyTorch training loop
# ─────────────────────────────────────────────────────────────────────────────
